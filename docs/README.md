# 项目文档

本目录用于集中存放 **AIOrcTest** 的项目说明、架构说明、使用指南等文档。

## 建议结构

| 文件 / 子目录 | 说明 |
|---------------|------|
| `CHANGELOG.md` | 版本与改动记录（按时间或版本倒序） |
| 其他 `.md` | 按需新增，例如架构、部署、API 说明等 |

开发启动脚本见仓库根目录 `scripts/`（`start-backend.sh`、`start-frontend.sh`）。

新增文档时请在本文件中补充索引链接，便于查找。
